const number = [1, 2, 3, 4, 5, 6, 7, 8];

const joined = number.join(",");
console.log(joined); // 1,2,3,4,5,6,7,8 // Return a string

// Spliting
const message = "Hey my name is ankrish";
const parts = message.split(" ");

console.log(parts); // [ 'Hey', 'my', 'name', 'is', 'ankrish' ]
